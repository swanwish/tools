#!/bin/bash

rm ~/.bash_history && ln -s /dev/null ~/.bash_history
rm ~/.mysql_history && ln -s /dev/null ~/.mysql_history
rm ~/.psql_history && ln -s /dev/null ~/.psql_history
rm ~/.sqlite_history && ln -s /dev/null ~/.mysql_history

